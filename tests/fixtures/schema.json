{
  "type": "object",
  "properties": {
    "age": {
      "type": "integer",
      "default": 0,
      "minimum": 18,
      "maximum": 99,
      "sanitizers": [
        "ToInteger"
      ],
      "errorMessages": {
        "type": {
          "code": 2000,
          "message": "Parameter `age` should be numerical"
        },
        "minimum": {
          "code": 2000,
          "message": "Minimum `age` is 18"
        },
        "maximum": {
          "code": 2000,
          "message": "Maximum `age` is 99"
        }
      }
    }
  },
  "required": [
    "age"
  ]
}